## GeekBrains 
* **git init** - инициализация локального репозитория
* **git status** - получить информацию от ***git*** о его текущем состоянии
* **git add** - добавить файлы или файлы к следующему коммиту
* **git commit -m "massage"** - создание коммита
* **git log** - вывод на экран истории всех коммитов с их хеш-кодом
* **git checkout** - переход от одного коммита к другого
* **git checkout master** - вурнутся к актуальному состоянию и продолжить работу
* **git diff** - увидеть разницу между текущим файлом и закоммиченным файлом

* **gitbranch** - показывает текущую ветку 

*PAUSA*

* **git branch 'name new branch'** - создаем новую вутку

*PAUSA*


* **git checkout 'name branch'** - переходим на другую ветку, на любую ветку и тут создадим коенфликт

* **git merge 'name branch'** - слияние веток

* **git branch -d 'name branch'** - удаляем ветку

* **git log --graph** - показывает ветки ит их слияние

* **git commit -am 'comment'** - сразу добавляет и коммитит изменение

* **git clone 'address repo'** - копируем репозиторий с ***git hub***

* **git push** - отправляем изменения на ***git***

* **git pull** - зашружаем изменения с ***git***

## !! Во время загрузки происходит слияние веток, могут возникнуть конфликты 